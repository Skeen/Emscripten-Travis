language: cpp

dist: trusty
sudo: required

install: ./cmake.sh

before_script: ./emscripten_before.sh
script: ./emscripten.sh
after_script: ./emscripten_after.sh

cache:
    directories:
        - cmake-3.4.3/
        - /usr/local/share/cmake-3.4/
        - /usr/local/doc/cmake-3.4/
        - /usr/local/bin/
